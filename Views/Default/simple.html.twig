<!DOCTYPE html>
<html>
<head>
    <title>Email Threads</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h1>Email Threads</h1>
        
        <div class="alert alert-info">
            Email Threads Plugin is working!
        </div>
        
        <div class="card">
            <div class="card-header">
                <h3>Email Threads List</h3>
                <a href="{{ path('mautic_emailthreads_config') }}" class="btn btn-primary">Settings</a>
            </div>
            <div class="card-body">
                {% if threads is empty %}
                    <div class="alert alert-secondary">
                        <h4>No Email Threads Yet</h4>
                        <p>Email threads will appear here once you start sending emails to your contacts.</p>
                        <p><strong>To create threads:</strong></p>
                        <ol>
                            <li>Make sure the plugin is enabled in settings</li>
                            <li>Send an email to a contact</li>
                            <li>The thread will be created automatically</li>
                        </ol>
                    </div>
                {% else %}
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>Subject</th>
                                    <th>Contact</th>
                                    <th>Messages</th>
                                    <th>Last Message</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for thread in threads %}
                                    <tr>
                                        <td>{{ thread.subject }}</td>
                                        <td>
                                            {% if thread.lead %}
                                                {{ thread.lead.name ?: thread.lead.email }}
                                            {% else %}
                                                <em>Unknown Contact</em>
                                            {% endif %}
                                        </td>
                                        <td><span class="badge bg-primary">{{ thread.messageCount }}</span></td>
                                        <td>{{ thread.lastMessageDate|date('Y-m-d H:i') }}</td>
                                        <td>
                                            <a href="{{ path('mautic_emailthreads_view', {'id': thread.id}) }}" class="btn btn-sm btn-outline-primary">
                                                View
                                            </a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
</body>
</html>
