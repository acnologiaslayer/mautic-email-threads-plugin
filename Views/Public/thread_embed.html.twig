<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Thread: {{ thread.subject }}</title>
    <style>
        .email-thread-embed {
            max-width: 100%;
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            font-size: 14px;
            line-height: 1.4;
        }
        .thread-header-embed {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 6px;
            margin-bottom: 15px;
            border-left: 3px solid #007bff;
        }
        .message-item-embed {
            background: white;
            border: 1px solid #dee2e6;
            border-radius: 6px;
            margin-bottom: 12px;
            overflow: hidden;
        }
        .message-header-embed {
            background: #f8f9fa;
            padding: 12px;
            border-bottom: 1px solid #dee2e6;
            font-size: 12px;
        }
        .message-content-embed {
            padding: 15px;
            line-height: 1.5;
        }
        .message-meta-embed {
            font-size: 0.8rem;
            color: #6c757d;
        }
        .thread-footer-embed {
            margin-top: 15px;
            padding: 10px;
            background: #f8f9fa;
            border-radius: 4px;
            font-size: 11px;
            color: #6c757d;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="email-thread-embed">
        <div class="thread-header-embed">
            <h4 style="margin: 0 0 8px 0; font-size: 16px;">{{ thread.subject }}</h4>
            <div class="message-meta-embed">
                Conversation with {{ thread.lead ? (thread.lead.name ?: thread.lead.email) : 'Unknown' }}
                | {{ messages|length }} message{{ messages|length != 1 ? 's' : '' }}
                | Started {{ thread.firstMessageDate|date('M j, Y') }}
            </div>
        </div>

        <div class="messages-container">
            {% if messages is empty %}
                <div style="padding: 20px; text-align: center; color: #6c757d;">
                    <p>No messages found in this conversation thread.</p>
                </div>
            {% else %}
                {% for message in messages %}
                    <div class="message-item-embed">
                        <div class="message-header-embed">
                            <div style="display: flex; justify-content: space-between; align-items: start;">
                                <div>
                                    <strong>{{ message.subject }}</strong>
                                    <div class="message-meta-embed">
                                        From: {{ message.fromName ?: message.fromEmail }}
                                    </div>
                                </div>
                                <div style="text-align: right;">
                                    <div class="message-meta-embed">
                                        {{ message.dateSent|date('M j, Y \\a\\t g:i A') }}
                                    </div>
                                    <span style="background: #6c757d; color: white; padding: 2px 6px; border-radius: 3px; font-size: 10px;">
                                        {{ message.emailType|title }}
                                    </span>
                                </div>
                            </div>
                        </div>
                        <div class="message-content-embed">
                            {{ message.content|striptags('<p><br><strong><em><u><a><ul><ol><li><h1><h2><h3><h4><h5><h6>')|raw }}
                        </div>
                    </div>
                {% endfor %}
            {% endif %}
        </div>

        <div class="thread-footer-embed">
            Powered by Mautic Email Threads | Thread ID: {{ thread.threadId }}
        </div>
    </div>
</body>
</html>